name: Hello World
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "Hello, ${{vars.MY_NAME}} A demo build"
             echo "Docker has ${{ secrets.DOCKER_PASSWORD }}"
 
