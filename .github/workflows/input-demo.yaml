name: Inputs Demo
on: 
  workflow_dispatch: 
    inputs: 
      environment:
        description: Choose the environment to deploy
        required: true
        default: dev
jobs:
    inputs-demo:
      runs-on: ubuntu-latest
      steps:
        - name: DEploy the API
          run : echo "Deploying the App to ${{ inputs }}"
