name: Or Condition demo
on: push
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run for me or Java
        if: github.actor == 'ageorge2026' || github.actor == 'Java'
        run: echo "This step is triggered by ageorge2026 or Java"
